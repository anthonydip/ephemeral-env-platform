# Copy this file to .env and fill in your actual values:
#   cp .env.example .env

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
# Set the logging level for console output
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO

# Path to the log file for persistent logging
# Set to empty string ("") to disable file logging
# Default: logs/ephemeral-env.log
LOG_FILE=logs/ephemeral-env.log

# ----------------------------------------------------------------------------
# GitHub Integration (Required for PR comments)
# ----------------------------------------------------------------------------
# GitHub Personal Access Token for local testing
# Required scopes: repo (full control of private repositories)
# Note: In GitHub Actions, this is auto-provided as secrets.GITHUB_TOKEN
GITHUB_TOKEN=ghp_your_personal_access_token_here

# GitHub Repository in format: owner/repo-name
GITHUB_REPO=your-username/your-repo-name

# ----------------------------------------------------------------------------
# Infrastructure Configuration
# ----------------------------------------------------------------------------
# Public IP address of your Ingress host running k3s
# Used to generate preview URLs in PR comments
HOST_PUBLIC_IP=your.host.public.ip.here
