apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  stripPrefix:
    prefixes:
    {% for prefix in prefixes %}
      - {{ prefix }}
    {% endfor %}
